import pyglet, animation

# Player sprites

#image = pyglet.resource.image('dog.png')
#sheet = pyglet.image.TextureGrid(pyglet.image.ImageGrid(image, 1, 2))



# Standing sprite

standing = pyglet.resource.image('clara/standing.png')
#standing_image = pyglet.resource.image('clara/standing.png')
#standing_sheet = pyglet.image.TextureGrid(pyglet.image.ImageGrid(standing_image, 1, 2))
#standing = animation.make_from(standing_sheet, range(0, 2), [0.5])